Yet another boring researcher website

___
### _Note for myself_

- Solo hace falta un comando, y solo si actualizo algo del css. 
- El que se edita es el src.css, y luego se compila asi:
```bash
npx @tailwindcss/cli -i ./src.css -o ./output.css --minify
```
